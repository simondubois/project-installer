
<VirtualHost *:80>

    ServerName {{ hostname }}

    DocumentRoot "{{ public_path }}"

    <Directory {{ public_path }}>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

</VirtualHost>
