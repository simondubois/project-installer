
<VirtualHost *:80>

    ServerName {{ hostname }}

    DocumentRoot "{{ public_path }}"

    <Directory {{ public_path }}>
        Options Indexes FollowSymLinks
        AllowOverride FileInfo
        Require all granted
    </Directory>

</VirtualHost>
